{
  "type": "object",
  "required": [
    "job_id",
    "enqueued_at",
    "trigger_class",
    "episode_paths",
    "max_recent_episodes",
    "min_episode_signals",
    "status",
    "attempts",
    "last_polled_at",
    "last_started_at",
    "last_finished_at",
    "last_result",
    "run_id",
    "error"
  ],
  "properties": {
    "job_id": {"type": "string"},
    "enqueued_at": {"type": "string", "format": "date-time"},
    "trigger_class": {"type": "string"},
    "episode_paths": {"type": "array", "items": {"type": "string"}},
    "max_recent_episodes": {"type": ["integer", "null"]},
    "min_episode_signals": {"type": ["integer", "null"]},
    "status": {"type": "string"},
    "attempts": {"type": "integer", "minimum": 0},
    "last_polled_at": {"type": ["string", "null"], "format": "date-time"},
    "last_started_at": {"type": ["string", "null"], "format": "date-time"},
    "last_finished_at": {"type": ["string", "null"], "format": "date-time"},
    "last_result": {"type": ["string", "null"]},
    "run_id": {"type": ["string", "null"]},
    "error": {"type": ["string", "null"]}
  },
  "additionalProperties": false
}
