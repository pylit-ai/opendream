# Execution plan

1. Add canonical and proposal schema assets for activation and repair.
2. Add a first-class `activate` command plus `init --activate-configured`.
3. Implement registry persistence, drift checks, and repair.
4. Install native managed surfaces for Claude Code and OpenClaw.
5. Install AGENTS plus wrapper surfaces for Codex.
6. Add doctor coverage and release smoke for activation.
7. Promote activation as the standard path in docs.
