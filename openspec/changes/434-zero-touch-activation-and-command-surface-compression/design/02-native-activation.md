# Native activation

Use the target's actual lifecycle surface where it exists.

- Claude Code: native hooks and settings
- Codex: AGENTS plus repo-local wrapper fallback
- OpenClaw: configured runtime hooks and prompts

Reference scripts under `.meta/spec-adapters/` remain examples, not the standard setup path.
