# Migration

Older service or autowire entrypoints remain functional for a deprecation window but should point operators to:
- `init --activate-configured`
- `activate`
- `status`
- `activate --repair`
- `deactivate`
