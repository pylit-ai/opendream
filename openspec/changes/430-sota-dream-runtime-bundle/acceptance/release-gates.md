# Release gates

- [x] consolidation emits validated plan and verifier artifacts
- [x] queued dream jobs survive restart and drain correctly
- [x] adapter examples expose the worker path
- [x] release smoke runs `dream enqueue` and `dream worker --once`
