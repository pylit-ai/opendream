# Spec: Service Status Surface

- CLI MUST expose JSON status and doctor output
- CLI SHOULD expose compact human-formatted status and doctor output
- status MUST integrate manifest, runtime, worker health, and queue backlog
