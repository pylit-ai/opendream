# Spec: Supervisor Integration

- launchd user services MUST be supported on macOS
- systemd user services MUST be supported on Linux
- user mode MUST remain the default
- system mode MUST require explicit opt-in
